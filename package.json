{
  "name": "subgraph-assignment",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ subgraph-assignment",
    "create-local": "graph create --node http://localhost:8020/ subgraph-assignment",
    "remove-local": "graph remove --node http://localhost:8020/ subgraph-assignment",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 subgraph-assignment",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.65.0",
    "@graphprotocol/graph-ts": "^0.32.0",
    "@types/bn.js": "^5.1.5"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
